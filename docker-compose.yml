version: "3"
# services:
#   rabbitmq:
#     image: rabbitmq:3-management
#     container_name: rabbitmq
#     hostname: rabbitmq
#     volumes:
#       - /var/lib/rabbitmq
#     ports:
#       - "5672:5672"
#       - "15672:15672"
#     env_file:
#       - ./rabbitmq.env

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: transfer-message-tool
    environment:
      - PORT=${PORT}
    ports:
      - '3300:3300'
    volumes:
      - ./src:/app/src
